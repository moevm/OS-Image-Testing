## Инструменты тестирования

|Инструмент|Что можно конфигурировать|Накладные расходы по памяти от установки|Накладные расходы от запуска|Переносимость по архитектурам процессоров (arm, x86, risc-V)
|-------------|-------------|-------------|-------------|-------------|
|stress-ng||~2-4 МБ|Процессор: от среднего до предельного. Если включены вычислительные сценарии, загрузка упирается во все доступные ядра. Оперативная память: от низкой до высокой. При сценариях давления на память расход растёт пропорционально числу рабочих процессов и объёму памяти на каждый процесс. Диск: от нулевого до высокого. При дисковых сценариях создаётся большое количество операций чтения/записи, включая мелкие операции, которые нагружают подсистему хранения.|Поддерживает все архитектуры
|sysbench|Количество рабочих потоков, длительность теста, целевая интенсивность, лимит событий, сложность CPU-задачи, объём и размер блока памяти, режим файловой нагрузки, объём файлов, число файлов, размер файлового блока, тип случайности, драйвер базы данных.|~500 КБ|Процессор: базовый запуск лёгкий, нагрузка растёт линейно с числом параллельных исполнителей до упора в ядра и блокировки. Оперативная память: умеренная, растёт из-за служебных данных каждого исполнителя и из-за размера рабочего набора данных. Диск: в файловых сценариях может стать высоким, особенно при случайном доступе и большом объёме данных.|Поддерживает x86; для ARM, RISC-V используется сборка из исходников или пакеты дистрибутива(явно не заявлен).
|fio||~1,5-2,5 МБ|Процессор: от низкого до высокого. Растёт при большом числе параллельных заданий, глубокой очереди операций и включённой проверке данных.Оперативная память: умеренная, увеличивается вместе с числом параллельных заданий и глубиной очереди. Диск: может полностью занять устройство хранения, особенно на случайном доступе и маленьком размере блока.|Поддерживает все архитектуры
|vdbench|Описание устройств, описание профилей нагрузки, описание прогонов, количество потоков, размер операций, целевая скорость, длительность теста, доля случайного доступа, баланс чтения и записи, параметры открытия файлов, дедупликация.|Архив 5 МБ + накладные расходы на Java|Процессор: на старте есть постоянный расход из-за среды выполнения, затем растёт от интенсивности генерации нагрузки и сбора статистики.Оперативная память: стартовый уровень выше, чем у более простых утилит, дополнительно растёт с усложнением профиля нагрузки и количеством одновременно активных задач. Диск: высокий при интенсивных профилях хранения.|Переносимость определяется Java-окружением и ОС: инструмент запускается там, где поддерживается требуемая Java и доступны его зависимости.
|ioping|Количество запросов, интервал между запросами, размер запроса, время прогона, асинхронный режим, прямой режим доступа, кэшируемый режим, пакетный режим, принудительная синхронизация, ускоренный режим, режим записи.|50-100 КБ|Процессор: низкий, растёт при частых замерах и в режимах записи/случайного доступа. Оперативная память: низкая. Диск: от нулевого до высокого, если включена запись, случайный доступ и короткий интервал между запросами.|Поддерживает все архитектуры
|iperf3|Количество параллельных потоков, длительность теста, протокол передачи, целевой битрейт, направление трафика, размер сетевого окна, привязка процесса к ядрам CPU, режим передачи данных с минимальным копированием между памятью приложения и ядра операционной системы, формат и детализацию вывода результатов.|80–150 КБ|Процессор: от умеренного до высокого на больших скоростях передачи, заметно растёт при нескольких параллельных потоках и двустороннем обмене. Оперативная память: умеренная, растёт с параллельностью и объёмом буферов.| Поддерживает все архитектуры
|perf bench|Выбор подсистемы и конкретного теста, число повторов прогона, формат вывода результатов, параметры конкурентности и интенсивности внутри выбранного микро-теста, включая число потоков, число циклов, объём обрабатываемых данных, размеры рабочих буферов, для сценариев обмена между потоками и очередями задач — параметры читателей, писателей и групп исполнения.|~21 МБ|Процессор: высокий в сценариях, где много переключений и синхронизации между задачами.Оперативная память: от низкой до умеренной, заметнее растёт в тестах подсистемы памяти и межузлового доступа памяти.|Поддерживает все архитектуры
|chaosblade|Интенсивность CPU в процентах, количество нагружаемых CPU, выбор CPU, время воздействия, сценарии деградации памяти, диска, сети и процессов.|~75 МБ|Процессор: собственный управляющий слой лёгкий, расход создаёт внедряемый сценарий отказа. Оперативная память: низкая, может стать очень высокой, если сценарий специально создаёт давление на память. Диск: может резко вырасти при сценариях деградации операций хранения. Сеть: может резко вырасти или ухудшиться по качеству связи при сетевых сценариях.|Поддерживает x86, arm; risc-V сборка из исходников (явно не заявлен)
|pgbench|Количество клиентов, количество рабочих потоков, длительность, количество транзакций на клиента, скорость генерации транзакций, лимит задержки, тип протокола, переподключение на транзакцию, прогресс, журналирование запросов и задержек.|~5 МБ|Процессор: растёт на стороне генератора нагрузки и на стороне сервера базы данных, при большом числе одновременных клиентов может занять все доступные ядра. Оперативная память: у генератора нагрузки умеренная, у сервера базы может расти из-за кэшей, рабочих областей и множества соединений. Диск: высокий, особенно в сценариях с интенсивной записью и журналированием. Сеть: высокая при удалённом сервере базы и большом числе клиентов.|Поддерживает все архитектуры
|LTP|Выбор сценариев или файлов тестов, фильтрация, исключение тестов, длительность прогона, рандомизация порядка, количество параллельных экземпляров, параметры фоновой нагрузки на CPU/IO/память/диск, выбор тестового устройства и типа ФС, пути и форматы логов.|150–620 МБ (в зависимости от конфигурации)|Процессор: управляющий слой умеренный, наборы тестов ядра могут давать очень высокую и длительную нагрузку. Оперативная память: зависит от выбранных групп, часть тестов специально проверяет поведение при нехватке памяти. Диск: может быть высоким в проверках файловых систем и ввода-вывода.|Для тестов поддержка архитектуры задаётся по каждому тесту отдельно|
|PTS|Выбор тестов и наборов тестов, запуск полного прогона или только выбранной части, автоматический пакетный запуск, стрессовый многопроцессный запуск, число одновременно выполняемых тестов, правила автоматического поведения прогона, установка необходимых компонентов для выбранных тестов.|Базовый фреймворк: 700 КБ, дальнейший размер зависит от тестов|Процессор: собственный запуск лёгкий, итоговая нагрузка определяется тестами, которые он запускает внутри себя, при тяжёлых профилях высокий расход. Оперативная память: расходуют конкретные тесты, а не управляющий слой. Диск: от низкого до высокого в зависимости от выбранных тестов. Сеть: умеренная, растёт при сетевых тестах и при работе с удалёнными результатами.|Поддерживает все архитектуры
|fwts|Выбор архитектуры и групп тестов, запуск всех тестов или только выбранных, исключение тестов, сценарии перехода в сон и гибернацию, количество циклов и паузы между циклами, пороги времени для операций засыпания и пробуждения, путь к данным таблиц прошивки и входным диагностическим файлам.|~500-1500 КБ|Процессор: от низкого до умеренного, в расширенных проверках может стать выше. Оперативная память: от низкой до умеренной, растёт при широком наборе проверок и подробном журналировании. Диск: умеренный расход на логи и отчёты при подробной диагностике.|Поддерживает все архитектуры
|xfstests|Выбор основного тестового диска и каталога монтирования, выбор отдельного временного диска и его точки монтирования для рискованных проверок, выбор типа файловой системы, подключение дополнительных дисков для журнала и выделенной области реального времени, запуск выбранных групп или отдельных тестов, исключение тестов, случайный порядок выполнения, число повторов и масштаб нагрузки, выбор режимов для локальных и сетевых файловых систем.|Исходный код ~1 МБ, требуется тестовое пространство 5-20 ГБ|Процессор: управляющий слой лёгкий, проверки файловых систем могут нагружать процессор. Оперативная память: умеренная, растёт в тяжёлых наборах и при высоком параллелизме. Диск: высокий и длительный для подсистемы хранения.|Не имеет официальных ограничений по архитектурам
|Hardinfo2|Выбор конкретного встроенного бенчмарка, режим только сбора системной информации без запуска бенчмарков, ограничение количества результатов для сравнения, формат представления результатов, пользовательская метка для группировки, подробный или тихий режим вывода.|~10 МБ|Процессор: в режиме инвентаризации низкий, в режиме встроенных проверок производительности от умеренного до высокого. Оперативная память: низкая в базовом режиме, растёт при включении тяжёлых проверок и формировании детальных отчётов. Диск: низкий, увеличивается при сохранении больших отчётов и запуске тестов, связанных с хранением.|Поддерживает все архитектуры