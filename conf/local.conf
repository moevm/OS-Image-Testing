MACHINE ??= "qemux86-64"

DL_DIR ?= "/home/user/poky/downloads"

SSTATE_DIR ?= "/home/user/poky/sstate-cache${TOPDIR}/sstate-cache"

DISTRO ?= "poky"

PACKAGE_CLASSES ?= "package_rpm"

IMAGE_FEATURES += "serial-autologin-root empty-root-password"

USER_CLASSES ?= "buildstats"

PATCHRESOLVE = "noop"

BB_DISKMON_DIRS ??= "\
    STOPTASKS,${TMPDIR},1G,100K \
    STOPTASKS,${DL_DIR},1G,100K \
    STOPTASKS,${SSTATE_DIR},1G,100K \
    STOPTASKS,/tmp,100M,100K \
    HALT,${TMPDIR},100M,1K \
    HALT,${DL_DIR},100M,1K \
    HALT,${SSTATE_DIR},100M,1K \
    HALT,/tmp,10M,1K"

PACKAGECONFIG:append:pn-qemu-system-native = " dtc fdt"

CONF_VERSION = "2"

DISTRO_FEATURES:append = " ptest"

IMAGE_INSTALL:append = " ptest-runner image-test tests tests-ptest glibc stress-ng wget auto-login"

THREADS_AMOUNT = "3"

BB_NUMBER_THREADS = "${THREADS_AMOUNT}"

PARALLEL_MAKE = "-j ${THREADS_AMOUNT}"

BB_SERVER_TIMEOUT = "600"

CONNECTION_TIMEOUT = "600"

PKG_CONFIG_PATH:append = ":${STAGING_LIBDIR_NATIVE}/pkgconfig"

OECORE_NATIVE_SYSROOT = "${STAGING_DIR_NATIVE}"

DEPENDS:append:pn-iproute2 = " libtirpc"

EXTRA_USERS_PARAMS = "usermod -p '*' root;"

EXTRA_IMAGE_FEATURES += "ptest-pkgs"
